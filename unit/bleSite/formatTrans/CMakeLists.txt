FILE(GLOB src "*.cpp")
add_library(format STATIC ${src})
target_include_directories(format INTERFACE ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_link_libraries(format PUBLIC qlibc)
target_link_libraries(format PUBLIC serial)
target_link_libraries(format PUBLIC log)
